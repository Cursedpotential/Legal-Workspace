# Task A: complete the toolkit capability port map

Owner authorized execution with cheaper parallel agents on 2026-09-20.
This supersedes the earlier prompt's blanket prohibition on agents.

Repository verified at `E:/AI_Workspace/Projects/Propria/modules/Legal-desktop`.
The root router dated 2026-09-20 supersedes the older path in this plan.

## Copyable execution prompt

Complete Task A in docs/planning/2026-09-13-advocatio-reconciliation/ROADMAP.md.
Reuse the saved research. Read applicable instructions, check Git ownership and
dirty state, and query related Docstore records. Do not repeat the broad audit.

Use three cheaper capable agents with separate output ownership:

1. callable: tools, commands, hooks, utilities, maintenance, server/runtime
   adapters, sidecar routes and widgets.
2. guidance: skills, agent roles, procedures and MCP prompts.
3. references: legal-source records, resource/event packs, MCP resources and
   external-service assessments.

The coordinator runs A-partition.py first. It pins the input SHA-256 and assigns
every input entry exactly once. Workers read only their assigned JSON, the toolkit
report, MCP-CLIENT-AND-PORTS.md and LANGUAGE-CONVERGENCE.md. Targeted additional
reading is allowed only when needed to resolve an individual entry.

Workers own only parts/<section>.csv and parts/<section>-receipt.md. They are not
alone in the repository: preserve other edits, do not alter another worker's
files, and do not edit the final map or roadmap. Do not spawn more workers.

Preserve source identity and origin/version/path. Map each capability to a
proposed destination and integration approach, explain human and agent access,
dependencies, missing information, required verification and the next action.
Retain observed implementation/wiring separately from proposed routes. The
inventory is a dated observation, not proof of current execution or legal validity.
Do not silently collapse variants or invent tool availability. Preserve evidence
custody upstream; operational extraction is not Advocatio evidence acceptance.

Use exactly the columns in parts/assignment.json. Destination values are resource,
method, mcp_tool, shared_code, operational_support, unresolved. Approach values
are reuse_connection, expose_implementation, import_reconcile, adapt, port,
investigate. Make each row actionable; use concise flags for unknowns.

The coordinator merges parts in original inventory order and verifies exact
coverage, identity/provenance preservation, schema, allowed values and nonempty
required fields. Review ambiguous or conflicting classifications before accepting
the map. Final outputs: A-capability-port-map.csv, A-receipt.md and
A-verification.json. Update Task A's State cell in ROADMAP.md and any directly
contradicted handoff sentence; do not rewrite the overall plan.

This task is documentation and integration planning. Do not install dependencies,
execute toolkit operations, change application code, register servers or modify
application databases. Governed Docstore publication of these task records is
authorized. Never permanently delete files or change host power state.

Save each completed section immediately. Record exact completed and outstanding
source IDs, file paths, checks and next step in its receipt. On resumption, verify
input hashes and reuse finished sections. Do not automatically begin Task B.

Publish final task documents and notes to Docstore through the installed governed
workflow; independently read back identities and content hashes. Record any
publication failure precisely, without claiming search indexing occurred. Commit
and push only this task's explicit file list, preserving all unrelated work.
